let a =5;
// export default a; // use single export
let x = 5;
let y = a + x;
export default  y; // defult export only one thing


import {pi, grret, name, message,user  } from "./app.js";
import d from "./second.js"

const person:user = {
    id:"knk",
    name:1233,
}
console.log(person);

import * as abc from "./app.js"
let oop = y + d 
// oop + z // error only we use imort items 